<%= simple_form_for :filter,
                          url: tacos_path,
                          method: :get,
                          html: { class: 'form-horizontal' },
                          defaults: { required: false } do |f| %>

  <div class="field">
    <table class="grid-table">
      <%= f.input :name %>
      <%= f.input :spicy, collection: ["All", true, false], selected: object[:spicy] %>
      <%= f.input :meat, collection: ["All", "Pork", "Beef", "Chicken"], selected: object[:meat] %>
    </table>
  </div>

  <div class="actions">
    <%= f.button :submit, "Filter", class: "btn-info" %>
  </div>
<% end %>
